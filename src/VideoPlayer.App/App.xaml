<Application x:Class="VideoPlayer.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Application.Resources>
    <SolidColorBrush x:Key="BgBrush" Color="#FF050505"/>
    <SolidColorBrush x:Key="PanelBrush" Color="#FF0E0E0E"/>
    <SolidColorBrush x:Key="ChromeBrush" Color="#FF0E0E0E"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#66222222"/>
    <SolidColorBrush x:Key="TextBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="MutedBrush" Color="#FF8A8A8A"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="HoverBrush" Color="#14FFFFFF"/>
    <SolidColorBrush x:Key="PackPanelBrush" Color="#E60E0E0E"/>
    <SolidColorBrush x:Key="PackAccentBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="PackTextBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="PackMutedBrush" Color="#FF8A8A8A"/>
    <SolidColorBrush x:Key="PackHairlineBrush" Color="#66222222"/>
    <FontFamily x:Key="PackFont">Segoe UI Variable, Segoe UI</FontFamily>
    <DropShadowEffect x:Key="OverlayShadow" Color="#FF000000" BlurRadius="8" ShadowDepth="0" Opacity="0.85"/>
    <DropShadowEffect x:Key="PackSheetShadow" Color="#FF000000" BlurRadius="18" ShadowDepth="0" Opacity="0.45"/>
    <SolidColorBrush x:Key="CaptureSheetBrush" Color="#FF0E0E0E"/>
    <SolidColorBrush x:Key="CaptureAccentBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="CaptureTextBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="CaptureMutedBrush" Color="#FF8A8A8A"/>
    <SolidColorBrush x:Key="CaptureOnAccentBrush" Color="#FF050505"/>
    <SolidColorBrush x:Key="CaptureBorderBrush" Color="#66222222"/>
    <SolidColorBrush x:Key="CaptureHoverBrush" Color="#14FFFFFF"/>
    <DropShadowEffect x:Key="CaptureSheetShadow" Color="#FF000000" BlurRadius="28" ShadowDepth="8" Opacity="0.55"/>
    <FontFamily x:Key="CaptureFont">Segoe UI Variable, Segoe UI</FontFamily>
    <Style TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="10,6"/>
      <Setter Property="Margin" Value="4,0"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="2" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#FF2A2A2A"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="Menu">
      <Setter Property="Background" Value="{StaticResource ChromeBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    </Style>
    <Style TargetType="MenuItem">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="{StaticResource ChromeBrush}"/>
    </Style>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    </Style>
    <Style TargetType="ListBox">
      <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
    </Style>
    <SolidColorBrush x:Key="SkinABgBrush" Color="#FF050505"/>
    <SolidColorBrush x:Key="SkinAPanelBrush" Color="#FF0E0E0E"/>
    <SolidColorBrush x:Key="SkinABorderBrush" Color="#66222222"/>
    <SolidColorBrush x:Key="SkinATextBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="SkinASecondaryBrush" Color="#FF8A8A8A"/>
    <SolidColorBrush x:Key="SkinAAccentBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="SkinAOnAccentBrush" Color="#FF050505"/>
    <SolidColorBrush x:Key="SkinAPlayTriangleBrush" Color="#FFE10600"/>
    <SolidColorBrush x:Key="SkinAThumbBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="SkinAHoverBrush" Color="#14FFFFFF"/>
    <SolidColorBrush x:Key="SkinAChromeFillBrush" Color="#14FFFFFF"/>
    <SolidColorBrush x:Key="SkinATrackRestBrush" Color="#FF3A3A3E"/>
    <BlurEffect x:Key="SkinAChromeBlur" Radius="20" KernelType="Gaussian"/>
    <Style x:Key="SkinATitleText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource SkinATextBrush}"/>
      <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
      <Setter Property="FontSize" Value="20"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style x:Key="SkinABodyText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource SkinATextBrush}"/>
      <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="FontWeight" Value="Regular"/>
    </Style>
    <Style x:Key="SkinAMetaText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource SkinASecondaryBrush}"/>
      <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="FontWeight" Value="Regular"/>
    </Style>
    <Style x:Key="IconButton" TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource SkinATextBrush}"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Width" Value="32"/>
      <Setter Property="Height" Value="32"/>
      <Setter Property="Padding" Value="6"/>
      <Setter Property="Margin" Value="4,0"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="2"
                    Width="32" Height="32">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{StaticResource SkinAHoverBrush}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.35"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="SkinATextButton" TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource SkinATextBrush}"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="10,6"/>
      <Setter Property="Margin" Value="4,0"/>
      <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="FontWeight" Value="Regular"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="2"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{StaticResource SkinAHoverBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="SkinAPlayButton" TargetType="Button">
      <Setter Property="Width" Value="40"/>
      <Setter Property="Height" Value="40"/>
      <Setter Property="Margin" Value="8,0"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Width="40" Height="40" CornerRadius="2" Background="Transparent">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="RailButton" TargetType="Button">
      <Setter Property="Width" Value="28"/>
      <Setter Property="Height" Value="28"/>
      <Setter Property="Margin" Value="0"/>
      <Setter Property="Padding" Value="2"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="HorizontalAlignment" Value="Center"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="Transparent" CornerRadius="2" Width="28" Height="28">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{StaticResource SkinAHoverBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="NextCtaButton" TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource SkinATextBrush}"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderBrush" Value="{StaticResource SkinATextBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="FontWeight" Value="Regular"/>
      <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="2" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{StaticResource SkinAHoverBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="CaptureSheetButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Foreground" Value="{StaticResource CaptureTextBrush}"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderBrush" Value="{StaticResource CaptureBorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="10,6"/>
      <Setter Property="Margin" Value="0"/>
      <Setter Property="FontFamily" Value="{StaticResource CaptureFont}"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="2" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#14FFFFFF"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="CaptureStepperButton" TargetType="Button" BasedOn="{StaticResource CaptureSheetButton}">
      <Setter Property="Width" Value="28"/>
      <Setter Property="Height" Value="28"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="FontSize" Value="14"/>
    </Style>
    <Style x:Key="CaptureStartButton" TargetType="Button" BasedOn="{StaticResource CaptureSheetButton}">
      <Setter Property="Background" Value="{StaticResource CaptureAccentBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Foreground" Value="{StaticResource CaptureOnAccentBrush}"/>
      <Setter Property="Padding" Value="18,8"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}"
                    BorderThickness="0" CornerRadius="2" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Opacity" Value="0.92"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="CaptureFormatButton" TargetType="Button" BasedOn="{StaticResource CaptureSheetButton}">
      <Setter Property="MinWidth" Value="52"/>
      <Setter Property="Padding" Value="12,6"/>
      <Setter Property="Margin" Value="0,0,6,0"/>
    </Style>
    <FontFamily x:Key="ClipFont">Segoe UI Variable, Segoe UI</FontFamily>
    <SolidColorBrush x:Key="ClipSheetBrush" Color="#FF0E0E0E"/>
    <SolidColorBrush x:Key="ClipTextBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="ClipMutedBrush" Color="#FF8A8A8A"/>
    <SolidColorBrush x:Key="ClipAccentBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="ClipOnAccentBrush" Color="#FF050505"/>
    <SolidColorBrush x:Key="ClipBorderBrush" Color="#66222222"/>
    <DropShadowEffect x:Key="ClipSheetShadow" Color="#FF000000" BlurRadius="24" ShadowDepth="0" Opacity="0.45"/>
    <Style x:Key="ClipSheetButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Foreground" Value="{StaticResource ClipTextBrush}"/>
      <Setter Property="Background" Value="#FF0E0E0E"/>
      <Setter Property="BorderBrush" Value="{StaticResource ClipBorderBrush}"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="Margin" Value="0"/>
      <Setter Property="FontFamily" Value="{StaticResource ClipFont}"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1" CornerRadius="2"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#14FFFFFF"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.45"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="ClipSaveButton" TargetType="Button" BasedOn="{StaticResource ClipSheetButton}">
      <Setter Property="Background" Value="{StaticResource ClipAccentBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource ClipAccentBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource ClipOnAccentBrush}"/>
    </Style>
    <Style x:Key="ClipFormatButton" TargetType="Button" BasedOn="{StaticResource ClipSheetButton}">
      <Setter Property="Padding" Value="10,6"/>
      <Setter Property="Margin" Value="0,0,6,0"/>
      <Setter Property="MinWidth" Value="72"/>
    </Style>
    <Style x:Key="ClipStepperButton" TargetType="Button" BasedOn="{StaticResource ClipSheetButton}">
      <Setter Property="Padding" Value="8,2"/>
      <Setter Property="MinWidth" Value="28"/>
      <Setter Property="Margin" Value="0"/>
    </Style>
    <Style x:Key="VerticalChromeSlider" TargetType="Slider">
      <Setter Property="Orientation" Value="Vertical"/>
      <Setter Property="Minimum" Value="0"/>
      <Setter Property="IsMoveToPointEnabled" Value="True"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Slider">
            <Grid HorizontalAlignment="Center">
              <Border Width="4" Background="{StaticResource SkinATrackRestBrush}" CornerRadius="2" Margin="0,6"/>
              <Track x:Name="PART_Track" Orientation="Vertical">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="Slider.DecreaseLarge" IsHitTestVisible="False">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Width="4" Background="{StaticResource SkinAAccentBrush}" CornerRadius="2"
                                HorizontalAlignment="Center" Margin="0,0,0,6"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb Width="12" Height="12">
                    <Thumb.Template>
                      <ControlTemplate TargetType="Thumb">
                        <Rectangle Width="12" Height="12" Fill="{StaticResource SkinAThumbBrush}"/>
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="Slider.IncreaseLarge">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" Width="16"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.IncreaseRepeatButton>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="ChromeSlider" TargetType="Slider">
      <Setter Property="Foreground" Value="{StaticResource SkinATextBrush}"/>
      <Setter Property="Minimum" Value="0"/>
      <Setter Property="IsMoveToPointEnabled" Value="True"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Slider">
            <Grid VerticalAlignment="Center">
              <Border Height="4" Background="{StaticResource SkinATrackRestBrush}" CornerRadius="2" Margin="6,0"/>
              <Track x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="Slider.DecreaseLarge" IsHitTestVisible="False">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Height="4" Background="{StaticResource SkinAAccentBrush}" CornerRadius="2" Margin="6,0,0,0"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb Width="12" Height="12">
                    <Thumb.Template>
                      <ControlTemplate TargetType="Thumb">
                        <Rectangle Width="12" Height="12" Fill="{StaticResource SkinAThumbBrush}"/>
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="Slider.IncreaseLarge">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" Height="16"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.IncreaseRepeatButton>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="SkipCapsuleButton" TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Foreground" Value="{StaticResource PackTextBrush}"/>
      <Setter Property="FontFamily" Value="{StaticResource PackFont}"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="Margin" Value="0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="SubtitleRowButton" TargetType="Button">
      <Setter Property="Background" Value="#220E0E0E"/>
      <Setter Property="BorderBrush" Value="#66222222"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Foreground" Value="{StaticResource PackTextBrush}"/>
      <Setter Property="FontFamily" Value="{StaticResource PackFont}"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Padding" Value="12,8"/>
      <Setter Property="Margin" Value="0,0,0,6"/>
      <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="2" Padding="{TemplateBinding Padding}">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#330E0E0E"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="CcOnButton" TargetType="Button" BasedOn="{StaticResource SkinATextButton}">
      <Setter Property="Foreground" Value="{StaticResource PackAccentBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource PackAccentBrush}"/>
    </Style>
  </Application.Resources>
</Application>
